#include <stdio.h>
/**
 * main - entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char lower_case;
for (lower_case = 'a'; lower_case <= 'z'; lower_case++)
putchar(lower_case);
{
if (alphabets == 'q')
continue;
else if (alphabets == 'e')
continue;
putchar(alphabets);
}
putchar('\n');
return (0);
}
